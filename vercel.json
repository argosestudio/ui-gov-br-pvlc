{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "buildCommand": "cd ui-pvlc && npm install && npm run build",
    "outputDirectory": "ui-pvlc/dist",
    "framework": "vite",
    "installCommand": "cd ui-pvlc && npm install",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ],
    "headers": [
        {
            "source": "/assets/(.*)",
            "headers": [
                {
                    "key": "Cache-Control",
                    "value": "public, max-age=31536000, immutable"
                }
            ]
        }
    ]
}